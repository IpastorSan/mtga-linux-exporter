#!/usr/bin/env python3
"""Thin executable wrapper — see cli.py."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
from cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
