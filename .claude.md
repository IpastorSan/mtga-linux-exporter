# Claude context

**Read `AGENTS.md` for the full runbook.** This file exists so Claude
picks up the same provider-agnostic instructions the rest of the agent
ecosystem uses (Codex, Cursor, aider, Cline, etc.).

No Claude-specific overrides at this time.
